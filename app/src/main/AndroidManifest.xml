<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flordelotus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".NewPostActivity"
            android:parentActivityName=".ArticlesActivity"></activity>

        <activity android:name=".AdminActivity"
        android:parentActivityName=".CalendarActivity"/>

        <activity android:name=".PeriodActivity"
            android:parentActivityName=".LoginActivity"/>

        <activity android:name=".CalendarActivity"/>

        <activity android:name=".ArticlesActivity"
            android:parentActivityName=".CalendarActivity"/>

        <activity android:name=".GenerateInvitationActivity"
            android:parentActivityName=".SettingsActivity"/>

        <activity android:name=".SettingsActivity"
            android:parentActivityName=".CalendarActivity"/>

        <activity android:name=".InputInvitationActivity"
            android:parentActivityName=".LoginActivity"/>

        <activity android:name=".RegisterActivity" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>